% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/misc.R
\name{select_cols}
\alias{select_cols}
\title{Select Metadata Columns from a Data Frame}
\usage{
select_cols(data, select_type, add_footr_tstamp = TRUE)
}
\arguments{
\item{data}{A data frame containing metadata (e.g., titles, footnotes, sources).}

\item{select_type}{A string indicating the type of columns to select. Can be \code{"TITLE"},
\code{"FOOTR"}, or a specific column name. If \code{NULL}, all columns are returned.}

\item{add_footr_tstamp}{Logical or a function. If \code{TRUE}, a function named \code{add_footr_tstamp()}
is called to append timestamps to footnotes. Defaults to \code{TRUE}.}
}
\value{
A list of selected columns from the input data, with \code{NA} values removed.
}
\description{
Selects specific columns from a metadata data frame based on the type of content desired
(e.g., titles, footnotes, or a named column). Optionally appends a timestamp to footnotes.
}
\details{
\itemize{
\item If \code{select_type} is \code{"TITLE"}, selects all columns starting with \code{"TTL"} and the \code{POPULATION} column.
\item If \code{"FOOTR"}, selects columns starting with \code{"FOOT"} and optionally appends timestamp from \code{SOURCE}.
\item If a specific column name is provided, attempts to select it directly.
\item If \code{select_type} is \code{NULL}, returns all columns.
}
}
\examples{
\dontrun{
select_cols(metadata_df, select_type = "TITLE")
select_cols(metadata_df, select_type = "FOOTR", add_footr_tstamp = TRUE)
select_cols(metadata_df, select_type = "PGMNAME")
}

}
