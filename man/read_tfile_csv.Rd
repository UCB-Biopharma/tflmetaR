% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{read_tfile_csv}
\alias{read_tfile_csv}
\title{Read Title and Footnote Metadata from a CSV File}
\usage{
read_tfile_csv(filename = NULL)
}
\arguments{
\item{filename}{A character string specifying the path to the CSV file.}
}
\value{
A data frame with column names converted to uppercase. The data frame should
contain at least the following required columns: \code{"PGMNAME"}, \code{"TTL1"}, \code{"SOURCE"}, and \code{"FOOT1"}.
}
\description{
Reads and validates a CSV file containing titles and footnotes for tables or figures,
and returns a data frame with standardized column names.
}
\details{
The function is typically used in reporting workflows to load external metadata
(titles, subtitles, footnotes, source notes, etc.) into the analysis environment.

This function performs the following steps:
\itemize{
\item Checks whether the file exists
\item Reads the CSV using \code{readr::read_csv()}
\item Converts all column names to uppercase
}

If the file does not exist or cannot be read, the function will stop with an informative error.
}
\examples{
\dontrun{
# Read a metadata file
df <- read_tfile_csv("data/titles_footnotes.csv")
head(df)
}

}
